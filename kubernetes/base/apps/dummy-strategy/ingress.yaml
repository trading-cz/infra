apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dummy-strategy
  namespace: trading-apps
  annotations:
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  ingressClassName: traefik
  rules:
  - host: dummy-strategy.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: dummy-strategy
            port:
              number: 8000
