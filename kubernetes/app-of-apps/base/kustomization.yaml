apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Root App of Apps - Base Configuration
# This includes all applications that should be deployed

resources:
  # Infrastructure components
  - ../../base/kafka

  # Application services
  - ../../base/apps

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/part-of: trading-system

# You can add more components here as you build them:
# - ../../base/monitoring
# - ../../base/ingestion
# - ../../base/strategies
