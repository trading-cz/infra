apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# PROD Environment - App of Apps
# Deploys all applications with production-specific settings

resources:
  # Use environment-specific overlays directly
  - ../../../overlays/prod/kafka
  - ../../../overlays/prod/apps

commonLabels:
  environment: prod
  deployment-method: app-of-apps
