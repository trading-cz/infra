apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: trading-apps

resources:
  - ../../../base/apps/alpaca-ingestion

commonLabels:
  environment: prod

patches:
  - path: configmap-patch.yaml
  - path: ingress-patch.yaml
  - path: deployment-patch.yaml
