apiVersion: v1
kind: ConfigMap
metadata:
  name: alpaca-ingestion-config
  namespace: trading-apps
data:
  # Override for dev environment
  LOG_LEVEL: "DEBUG"
  ENVIRONMENT: "development"
  
  # Dev-specific Kafka topics
  KAFKA_TOPIC: "dev-stock-ticks"
  
  # Dev-specific settings
  DEBUG: "true"
